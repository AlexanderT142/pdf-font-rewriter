{
  "name": "pdf-font-rewriter",
  "version": "0.1.22",
  "private": true,
  "license": "Apache-2.0",
  "description": "PDF Font Rewriter source and Obsidian desktop plugin package.",
  "scripts": {
    "build": "npm run build:obsidian",
    "build:obsidian": "npm --prefix obsidian-plugin run build",
    "test:helper-installer": "npm --prefix obsidian-plugin run test:helper-installer",
    "test:char-stats": ".venv/bin/python tests/test_char_stats.py",
    "typecheck:obsidian-root": "tsc -p tsconfig.json --noEmit --skipLibCheck"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "esbuild": "^0.25.4",
    "obsidian": "^1.8.7",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "pdfjs-dist": "^5.7.284"
  }
}
